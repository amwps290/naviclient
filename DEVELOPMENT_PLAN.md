# Navidrome Client 开发计划

本文档用于跟踪 Navidrome Client 从当前版本发展为日常可用桌面音乐播放器的开发进度。

## 使用规则

- `- [ ]` 表示尚未完成。
- `- [x]` 表示已经实现并通过验收。
- 每完成一个里程碑，应同时：
  - 勾选对应的实施任务和验收项。
  - 在里程碑标题下补充完成提交，例如：`完成提交：abc1234`。
  - 更新 README 中受影响的功能说明。
  - 保证旧配置能够继续加载，新增配置字段必须提供默认值。
- 所有 Cargo 验证命令最多使用 8 个并行任务，避免占满开发机器资源。

标准验证命令：

```bash
cargo fmt -- --check
cargo test -j 8
cargo clippy -j 8 --all-targets -- -D warnings
cargo build -j 8
```

## 当前能力基线

以下能力在制定本计划时已经具备，不作为待开发功能重复实施。

- [x] Subsonic 标准令牌认证和单服务器连接
- [x] 首页、艺术家、专辑、播放列表、收藏和搜索视图
- [x] 专辑封面加载和封面主题色提取
- [x] 播放、暂停、上一首、下一首和进度跳转
- [x] 基础播放队列和当前歌曲高亮
- [x] 列表循环、单曲循环和随机播放
- [x] Navidrome 收藏同步
- [x] 普通歌词和同步歌词加载
- [x] 同步歌词高亮、自动滚动和点击跳转
- [x] 原始音质与 128/192/256/320 kbps MP3 转码
- [x] HTTP Range、弱网缓冲、断点读取和磁盘音频缓存
- [x] 浅色、深色和跟随系统主题
- [x] 当前播放页面、动态封面配色和黑胶唱机视觉
- [x] 当前播放格式胶囊以及码率、文件大小 Tooltip
- [x] Windows、macOS、Linux 客户端窗口基础支持

---

# 第一阶段：播放器基础完整性

第一阶段完成后，播放器应具备日常播放所需的完整基础控制，并能在重启后恢复用户状态。

## 1. 音量、静音与持久化

优先级：P0

目标：为现有音频后端的 `SetVolume` 能力补充完整用户界面和状态管理，避免每次启动固定使用 100% 音量。

### 实施任务

- [ ] 在 `Config` 中增加音量字段，建议范围为 `0.0..=1.0`
- [ ] 为旧配置提供合理的默认音量，建议 `0.7`
- [ ] 增加静音状态或“静音前音量”状态
- [ ] 在播放器底栏增加音量图标按钮
- [ ] 增加音量滑块，支持即时调整
- [ ] 点击音量图标可以静音和恢复
- [ ] 根据音量级别显示静音、低音量和高音量图标
- [ ] 调整音量后持久化配置，避免高频重复写盘
- [ ] 应用启动时将保存的音量发送给音频线程
- [ ] 音量控制增加 Tooltip 和可访问名称

### 验收标准

- [ ] 重启应用后音量保持不变
- [ ] 静音后恢复到静音前的音量
- [ ] 静音不会把保存音量永久改为 0
- [ ] 播放中调整音量没有明显爆音或卡顿
- [ ] 最小窗口宽度下音量控件不会挤压播放控制
- [ ] 配置迁移测试、音量边界测试通过

## 2. 完整播放模式语义

优先级：P0

目标：补充“顺序播放且队列结束后停止”，并让所有模式的上一首、下一首和播放结束行为保持一致。

### 实施任务

- [ ] 为 `PlaybackMode` 增加 `Sequential` 或 `NoRepeat`
- [ ] 明确默认模式，建议使用顺序播放而不是列表循环
- [ ] 调整播放模式切换顺序和图标 Tooltip
- [ ] 顺序模式在最后一首结束后停止
- [ ] 列表循环在最后一首结束后回到第一首
- [ ] 单曲循环只在自然播放结束后重播当前歌曲
- [ ] 随机模式维护播放历史，而不是每次临时随机索引
- [ ] 随机模式的“上一首”返回实际播放过的歌曲
- [ ] 单曲队列和空队列行为有明确处理
- [ ] 将播放模式保存到配置或会话状态

### 验收标准

- [ ] 四种模式均有独立单元测试
- [ ] 顺序模式播放到队列末尾后不会自动重播
- [ ] 随机播放短时间内不会无意义重复同一首歌
- [ ] 随机模式下上一首和下一首行为符合播放历史
- [ ] 重启应用后保留用户选择的模式

## 3. 可编辑播放队列

优先级：P0

目标：将当前只读队列升级为可管理的播放队列。

### 实施任务

- [ ] 为歌曲提供“立即播放”操作
- [ ] 增加“下一首播放”操作
- [ ] 增加“添加到队列末尾”操作
- [ ] 支持从队列删除单首歌曲
- [ ] 支持清空当前歌曲之后的队列
- [ ] 支持清空整个队列，同时定义当前播放歌曲的处理方式
- [ ] 支持拖动或按钮调整队列顺序
- [ ] 当前播放歌曲在队列中保持明确标识
- [ ] 队列变化时正确维护 `queue_index`
- [ ] 删除当前歌曲时提供明确行为，不出现越界或停止异常
- [ ] 为专辑和播放列表提供“播放全部”和“追加全部”两种操作
- [ ] 为队列建立独立测试模块，覆盖增删、移动和索引修正

### 验收标准

- [ ] 队列增删和排序不会切错歌曲
- [ ] “下一首播放”始终插入当前歌曲之后
- [ ] 删除当前歌曲前后的上一首、下一首行为正确
- [ ] 空队列界面和按钮禁用状态正确
- [ ] 900px 最小窗口宽度下队列操作不重叠

## 4. 应用内键盘快捷键

优先级：P0

目标：不依赖鼠标完成常用播放操作。

### 默认快捷键

- [ ] `Space`：播放或暂停
- [ ] `Ctrl+Left`：上一首
- [ ] `Ctrl+Right`：下一首
- [ ] `Left`：快退 5 秒
- [ ] `Right`：快进 5 秒
- [ ] `Up`：提高音量
- [ ] `Down`：降低音量
- [ ] `M`：静音或恢复
- [ ] `L`：打开或关闭当前播放页/歌词页
- [ ] `Q`：打开或关闭队列
- [ ] `Ctrl+F`：聚焦搜索框
- [ ] `Escape`：关闭当前弹层或返回上一级

### 实施要求

- [ ] 输入框获得焦点时不拦截文本输入相关按键
- [ ] 快捷键行为复用现有播放方法，不复制业务逻辑
- [ ] 为快捷键增加集中定义和测试
- [ ] 不在主界面长期显示快捷键说明
- [ ] 通过按钮 Tooltip 或设置页按需展示快捷键

### 验收标准

- [ ] 所有快捷键在媒体库和当前播放页面均可用
- [ ] 搜索框、设置输入框中输入文本时不会误触播放操作
- [ ] 快退和快进不会越过歌曲起点或终点

## 5. 播放会话恢复

优先级：P0

目标：关闭并重新打开应用后恢复播放上下文，但默认保持暂停，避免启动后突然发声。

### 实施任务

- [ ] 定义独立的 `PlaybackSession` 持久化模型
- [ ] 保存当前歌曲 ID 和必要的歌曲快照
- [ ] 保存队列及当前索引
- [ ] 保存播放位置
- [ ] 保存播放模式
- [ ] 保存随机播放历史
- [ ] 在歌曲、队列或位置发生有意义变化时节流保存
- [ ] 正常退出前执行最后一次保存
- [ ] 启动时恢复队列、歌曲和位置
- [ ] 恢复后保持暂停，不自动播放
- [ ] 服务器已删除歌曲时跳过无效项并修正索引
- [ ] 会话文件损坏时安全回退，不影响应用启动

### 验收标准

- [ ] 重启后当前歌曲、队列和进度能够恢复
- [ ] 恢复过程不会自动发声
- [ ] 旧会话格式或损坏文件不会导致崩溃
- [ ] 切换服务器时不会恢复其他服务器的队列

---

# 第二阶段：Navidrome 与桌面系统集成

## 6. Navidrome 正在播放和 Scrobble 同步

优先级：P1

目标：让 Navidrome 正确记录正在播放内容、最近播放时间和播放次数。

### 实施任务

- [ ] 在 API 层实现 `updateNowPlaying`
- [ ] 在 API 层实现 `scrobble`
- [ ] 歌曲真正开始播放后发送正在播放状态
- [ ] 达到有效播放阈值后发送 Scrobble
- [ ] 自然播放结束时确保完成 Scrobble
- [ ] 短暂试听或快速跳过不计为完整播放
- [ ] 单曲循环时每轮只记录一次合理播放事件
- [ ] 请求失败时记录日志，不中断本地播放
- [ ] 评估是否需要有限次数的延迟重试
- [ ] 日志不得包含认证参数

### 建议有效播放阈值

满足以下任意条件即可：

- 播放时长达到歌曲总长度的 50%
- 播放时长达到 4 分钟

### 验收标准

- [ ] Navidrome 最近播放记录能够看到客户端播放行为
- [ ] 快速切歌不会错误增加播放次数
- [ ] 弱网或服务器不可用时本地播放不受影响
- [ ] API 参数构建和阈值判断有单元测试

## 7. 系统媒体键与媒体会话

优先级：P1

目标：支持键盘媒体键、蓝牙耳机和操作系统媒体面板。

### 平台范围

- [ ] Linux：MPRIS
- [ ] Windows：System Media Transport Controls
- [ ] macOS：Now Playing / MediaPlayer

### 公共能力

- [ ] 上报歌曲标题、艺术家和专辑
- [ ] 上报封面
- [ ] 上报播放、暂停和停止状态
- [ ] 上报播放位置和歌曲时长
- [ ] 接收播放、暂停、上一首、下一首命令
- [ ] 接收进度跳转命令
- [ ] 接收系统媒体键和蓝牙耳机按钮
- [ ] 平台集成失败时不影响主播放器启动
- [ ] 使用平台模块隔离条件编译代码

### 验收标准

- [ ] 至少一个主要平台完成端到端验证后再标记整个里程碑完成
- [ ] 锁屏或系统媒体面板能显示正确歌曲信息
- [ ] 外部播放控制与应用按钮状态同步
- [ ] 应用退出后系统媒体会话正确注销

## 8. 播放列表编辑

优先级：P1

目标：从只读播放列表升级为完整的 Navidrome 播放列表管理。

### API 能力

- [ ] 创建播放列表
- [ ] 重命名播放列表
- [ ] 删除播放列表
- [ ] 添加歌曲到播放列表
- [ ] 从播放列表移除歌曲
- [ ] 调整歌曲顺序

### 界面能力

- [ ] 新建播放列表对话框
- [ ] 歌曲上下文菜单中提供“添加到播放列表”
- [ ] 支持一次添加多首歌曲
- [ ] 播放列表详情页支持删除歌曲
- [ ] 播放列表详情页支持排序
- [ ] 删除和覆盖操作需要确认
- [ ] 操作成功后局部刷新相关数据
- [ ] API 失败时恢复乐观更新并显示错误

### 验收标准

- [ ] 新建、重命名、添加、删除和排序均与 Navidrome 同步
- [ ] 同一歌曲重复添加的行为有明确规则
- [ ] 并发操作不会导致本地列表与服务器长期不一致

## 9. 媒体库分页、排序与分类

优先级：P1

目标：保证大型音乐库下仍能高效浏览，而不是只加载固定数量的新专辑。

### 实施任务

- [ ] 专辑列表支持分页或增量加载
- [ ] 艺术家列表支持分页或虚拟化
- [ ] 增加最近添加
- [ ] 增加最近播放
- [ ] 增加经常播放
- [ ] 增加随机专辑
- [ ] 支持按名称、艺术家、年份排序
- [ ] 增加流派浏览
- [ ] 搜索结果支持按歌曲、专辑、艺术家过滤
- [ ] 支持刷新单个视图，不必每次全量刷新媒体库
- [ ] 多碟专辑按光盘号和曲目号正确排序
- [ ] 空状态、加载状态和分页失败状态完整

### 验收标准

- [ ] 具有数万首歌曲的媒体库仍可正常启动和浏览
- [ ] 增量加载期间界面不会长时间阻塞
- [ ] 排序和筛选不会破坏当前播放状态

---

# 第三阶段：音频质量与可靠性

## 10. ReplayGain 与音量标准化

优先级：P2

目标：降低不同歌曲和专辑之间的主观响度差异。

### 实施任务

- [ ] 调研 Navidrome/Subsonic 返回的 ReplayGain 字段兼容性
- [ ] 在歌曲模型中解析 Track Gain、Album Gain 和 Peak
- [ ] 增加关闭、Track、Album 三种模式
- [ ] 增加防削波处理
- [ ] 将增益应用在解码后、输出前
- [ ] 不影响用户主音量和静音逻辑
- [ ] 将模式保存到配置
- [ ] 缺少 ReplayGain 标签时安全回退

### 验收标准

- [ ] Track 和 Album 模式能产生可测量的增益差异
- [ ] 高峰值音频不会因增益产生明显削波
- [ ] 原始音质和转码流均可使用标准化

## 11. 下一首预加载与无缝播放

优先级：P2

目标：减少歌曲之间的等待和静音间隙，支持现场专辑、古典音乐和连续专辑。

### 实施任务

- [ ] 将音频工作线程改造为支持当前流和预备流
- [ ] 当前歌曲接近结束时预取下一首
- [ ] 提前创建下一首解码器和启动缓冲
- [ ] 当前歌曲结束后无缝切换到预备流
- [ ] 队列变化时取消失效预加载
- [ ] 转码质量变化时重新建立预加载
- [ ] 单曲循环复用或重新准备当前歌曲
- [ ] 失败时回退到现有普通切歌流程
- [ ] Gapless 完成后再评估可选 Crossfade

### 验收标准

- [ ] 连续曲目切换时没有明显网络等待
- [ ] 本地缓存命中时切换间隙接近不可感知
- [ ] 跳过歌曲或编辑队列不会播放已失效的预备歌曲
- [ ] 弱网下预加载不会抢占当前歌曲必要带宽

## 12. 音频输出设备管理与恢复

优先级：P2

目标：处理声卡、蓝牙耳机和默认输出设备变化。

### 实施任务

- [ ] 枚举可用输出设备
- [ ] 设置页提供输出设备选择
- [ ] 支持“跟随系统默认设备”
- [ ] 保存用户选择的设备
- [ ] 设备断开时暂停并显示可恢复错误
- [ ] 默认设备变化后自动重新连接
- [ ] 恢复设备后继续当前歌曲和位置
- [ ] 显示实际采样率和声道数供诊断
- [ ] 将设备生命周期与音频工作线程隔离

### 验收标准

- [ ] 拔出耳机或断开蓝牙不会导致应用崩溃
- [ ] 切换输出设备后当前队列和位置不丢失
- [ ] 不存在目标设备时能够回退到系统默认设备

## 13. 离线下载与缓存管理

优先级：P2

目标：在现有自动缓存基础上提供可见、可控的离线能力。

### 实施任务

- [ ] 设置页显示缓存总占用
- [ ] 支持配置缓存上限
- [ ] 支持清空全部缓存
- [ ] 支持清理单首歌曲或单张专辑缓存
- [ ] 显示当前歌曲缓存/下载进度
- [ ] 支持将专辑标记为离线可用
- [ ] 支持取消离线下载
- [ ] 区分原始音质和各转码档位缓存
- [ ] 下载任务支持暂停、恢复和失败重试
- [ ] 应用重启后恢复未完成下载任务
- [ ] 缓存清理不能删除正在播放或正在下载的文件

### 验收标准

- [ ] 完全断网时可以播放已标记离线的内容
- [ ] 缓存统计与磁盘实际占用基本一致
- [ ] 清理缓存不会破坏当前播放
- [ ] 下载任务失败时有明确状态和重试入口

---

# 第四阶段：桌面效率与高级体验

## 14. 歌曲上下文菜单

优先级：P3

### 实施任务

- [ ] 立即播放
- [ ] 下一首播放
- [ ] 添加到队列末尾
- [ ] 添加到播放列表
- [ ] 收藏或取消收藏
- [ ] 打开艺术家
- [ ] 打开专辑
- [ ] 查看歌曲和文件信息
- [ ] 删除缓存
- [ ] 根据上下文禁用不可用命令

### 验收标准

- [ ] 歌曲列表、搜索结果、队列和播放列表使用一致菜单
- [ ] 菜单命令复用公共业务方法
- [ ] 鼠标和键盘均可操作菜单

## 15. 系统通知与托盘

优先级：P3

### 实施任务

- [ ] 切歌时显示系统通知
- [ ] 通知显示封面、标题、艺术家和专辑
- [ ] 通知节流，连续快速切歌不会堆积
- [ ] 增加系统托盘图标
- [ ] 托盘菜单提供播放、暂停、切歌和退出
- [ ] 增加“关闭窗口时最小化到托盘”设置
- [ ] 明确真正退出和隐藏窗口的区别

### 验收标准

- [ ] 通知失败不会影响播放
- [ ] 托盘状态与窗口播放器状态同步
- [ ] 退出应用后托盘图标不会残留

## 16. 睡眠定时器

优先级：P3

### 实施任务

- [ ] 支持 15、30、45、60 分钟
- [ ] 支持当前歌曲结束后停止
- [ ] 支持当前队列结束后停止
- [ ] 显示剩余时间
- [ ] 支持取消和重新设置
- [ ] 最后 30 秒可选渐弱音量
- [ ] 定时器只影响播放，不自动退出应用

### 验收标准

- [ ] 休眠或系统时间变化不会导致明显错误
- [ ] 定时结束后播放状态和界面状态一致
- [ ] 渐弱不会永久修改用户保存的主音量

## 17. 多服务器与凭据安全

优先级：P3

目标：支持多个 Navidrome 实例，并避免在普通 JSON 文件中直接保存明文密码。

### 实施任务

- [ ] 将单服务器配置迁移为服务器配置列表
- [ ] 每个服务器使用稳定唯一 ID
- [ ] 支持新增、编辑、删除和切换服务器
- [ ] 每个服务器隔离播放会话和缓存命名空间
- [ ] 使用系统 Keyring 保存密码或令牌
- [ ] 配置文件只保存凭据引用
- [ ] 从旧版明文配置安全迁移
- [ ] 迁移完成后移除旧密码字段
- [ ] 明确处理系统 Keyring 不可用的情况
- [ ] 可选支持自签名证书，但必须展示安全警告

### 验收标准

- [ ] 切换服务器不会混用收藏、队列或封面
- [ ] 配置文件中不再出现明文密码
- [ ] 旧版本用户升级后无需重新输入全部配置
- [ ] 删除服务器时能够选择是否清理对应缓存

---

# 持续工程任务

以下任务不是单独功能，但每个里程碑都应执行。

## 测试与质量

- [ ] 新增业务逻辑必须有针对性单元测试
- [ ] 音频线程和队列状态变更必须覆盖异常路径
- [ ] API 参数构建必须测试，避免发送错误 Subsonic 参数
- [ ] 配置新增字段必须测试旧配置反序列化
- [ ] 运行 `cargo fmt -- --check`
- [ ] 运行 `cargo test -j 8`
- [ ] 运行 `cargo clippy -j 8 --all-targets -- -D warnings`
- [ ] 运行 `cargo build -j 8`

这些复选框属于每次开发的重复检查项，不应永久勾选；可在具体提交或 PR 描述中记录当次结果。

## 界面验收

- [ ] 1280x820 默认窗口检查
- [ ] 900x600 最小窗口检查
- [ ] 浅色主题检查
- [ ] 深色主题检查
- [ ] 中文和长英文文本检查
- [ ] 空状态、加载状态和错误状态检查
- [ ] 控件之间不存在重叠和意外布局跳动
- [ ] 图标按钮具有 Tooltip

## 安全与日志

- [ ] 日志不包含密码、令牌或完整认证 URL
- [ ] 错误信息对用户可理解，对日志保留诊断上下文
- [ ] 网络错误不会导致 UI 线程阻塞
- [ ] 文件写入使用安全路径并处理权限错误
- [ ] 配置和会话文件损坏时能够回退

---

# 建议实施顺序

严格按以下顺序推进可以减少返工：

1. 音量、静音与持久化
2. 完整播放模式语义
3. 可编辑播放队列
4. 应用内键盘快捷键
5. 播放会话恢复
6. Navidrome 正在播放和 Scrobble 同步
7. 系统媒体键与媒体会话
8. 播放列表编辑
9. 媒体库分页、排序与分类
10. ReplayGain 与音量标准化
11. 下一首预加载与无缝播放
12. 音频输出设备管理与恢复
13. 离线下载与缓存管理
14. 歌曲上下文菜单
15. 系统通知与托盘
16. 睡眠定时器
17. 多服务器与凭据安全

开发时允许根据真实用户反馈调整相邻项目顺序，但 P0 项目原则上应在 P1/P2 功能前完成。

---

# 里程碑完成定义

一个里程碑只有同时满足以下条件才能标记为完成：

- [ ] 所有必要实施任务已经完成
- [ ] 所有验收标准已经通过
- [ ] 新增逻辑具有合理测试覆盖
- [ ] 旧配置和旧缓存保持兼容，或提供迁移
- [ ] 浅色、深色和最小窗口完成视觉检查
- [ ] README 和本开发计划已经更新
- [ ] Cargo 四项标准验证通过，且并行任务不超过 8
- [ ] 已创建独立 Git 提交
- [ ] 里程碑下记录了完成提交号
